/**
 * User service — re-exports admin-level user operations.
 *
 * Kept for backward compatibility; prefer AdminService for user management.
 */

export { AdminService as UserService } from './adminService';
