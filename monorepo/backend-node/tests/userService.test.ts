/**
 * Placeholder — UserService tests migrated to authService.test.ts.
 *
 * See: tests/authService.test.ts for full auth coverage.
 */

describe('UserService (legacy)', () => {
  it('is covered by authService.test.ts', () => {
    expect(true).toBe(true);
  });
});
