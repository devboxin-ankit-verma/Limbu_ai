{
  "name": "@limbu/db",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:supplementary": "tsx scripts/apply-supplementary.ts",
    "db:seed": "tsx prisma/seed.ts",
    "db:neon:extensions": "tsx scripts/enable-neon-extensions.ts",
    "db:setup": "prisma migrate deploy && tsx scripts/apply-supplementary.ts && tsx prisma/seed.ts",
    "db:setup:neon": "tsx scripts/enable-neon-extensions.ts && prisma migrate deploy && tsx scripts/apply-supplementary.ts && tsx prisma/seed.ts",
    "db:setup:local": "prisma migrate deploy && tsx scripts/apply-supplementary.ts && tsx prisma/seed.ts",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0"
  },
  "devDependencies": {
    "prisma": "^6.9.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
